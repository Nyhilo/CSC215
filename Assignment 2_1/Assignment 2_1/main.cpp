/*
This program creates two arrays with 4 random numbers between 1 and 100 each.
It then creates a third array by adding the associated values of the other two arrays.
*/

#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

// Global variables
const int NUMBER_OF_VALUES;

// Function Prototypes
array createArray(int arraySize, int maxRange);
array mergeArrays(array firstArray, array secondArray);
void printHeader();


// Main function
int main()
{
	// Create two arrays populated with random numbers

	// Create the third array

	// Display Header

	//Print all arrays with the format "array1[1] + array2[1] = array3[1]"

	return 0;
}

// Function to create an array with n random numbers between 0 and x

// Function to create an array from two arrays with the associated values added together

// Header function